{% extends 'page.html.twig' %}

{% block content %}
{{ parent() }}

<h2>General</h2>
<ul>
    <li>
        <a href="{{ url_for('bookings') }}">Bookings</a>
    </li>
</ul>

<h2>Log In Required</h2>
<ul>
    <li>
        <p>
            <a href="{{ url_for('events') }}">Events</a>
{% if settings.schedule.parent_events %}
            <br>
            <small><a href="{{ settings.schedule.parent_events }}">Events in Sharepoint</a></small>
{% endif %}
        </p>
    </li>
    <li>
        <p>
            <a href="{{ url_for('events') }}">Reservations</a>
{% if settings.schedule.parent_reservations %}
            <br>
            <small><a href="{{ settings.schedule.parent_reservations }}">Reservations in Sharepoint</a>
{% endif %}
        </p>
    </li>
</ul>

{% endblock %}
